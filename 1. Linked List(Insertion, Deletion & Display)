1. Linked List(Insertion, Deletion & Display):
class Node {
    int data;
    Node next;
    Node(int data) {
        this.data = data;
        this.next = null;
    }
}
class LinkedList {
    Node head;
    // Insert at the end
    public void insert(int data) {
        Node newNode = new Node(data);
        if (head == null) {
            head = newNode;
            return;
        }
        Node current = head;
        while (current.next != null) {
            current = current.next;
        }
        current.next = newNode;
    }
    // Delete by key
    public void delete(int key) {
        Node current = head, prev = null;
        // Case 1: Key is in head
        if (current != null && current.data == key) {
            head = current.next;
            return;
        }
        // Case 2: Search for key
        while (current != null && current.data != key) {
            prev = current;
            current = current.next;
        }
        // Key not found
        if (current == null) return;
        // Unlink the node
        prev.next = current.next;
    }
    // Display the linked list
    public void display() {
        Node current = head;
        while (current != null) {
            System.out.print(current.data + " -> ");
            current = current.next;
        }
        System.out.println("null");
    }
}
public class Main {
    public static void main(String[] args) {
        LinkedList list = new LinkedList();
        list.insert(10);
        list.insert(20);
        list.insert(30);
        list.insert(40);
        System.out.println("Linked List:");
        list.display();
        System.out.println("After deleting 20:");
        list.delete(20);
        list.display();
        System.out.println("After inserting 50 (head):");
        list.insert(50);
        list.display();
    }
}
Output:
Linked List:
10 -> 20 -> 30 -> 40 -> null
After deleting 20:
10 -> 30 -> 40 -> null
After inserting 50 (head):
10 -> 30 -> 40 -> 50 -> null
